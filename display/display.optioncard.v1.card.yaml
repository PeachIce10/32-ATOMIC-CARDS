# ---------- Canonical Template Card ----------
id         : display.optioncard.v1
humanLabel : Option Card Selector
category   : DISPLAY
version    : 1

ELEMENTS:
  - Image-plus-Copy Card Tiles

CUSTOM-INPUTS:
  - cardList         : { id:string, label:string, subCopy:string, imageURL:URL, priceDeltaMinor:int }[]
  - multiSelect      : bool
  - maxSelect        : int | null
  - minSelect        : int | null
  - defaultCardIDs   : string[]
  - required         : bool
  - showAsEqualHeight: bool
  - orientation      : enum “row / column / grid”

# Buckets deferred to Pass B
LAYOUT   : TODO_LATER
RULES    : TODO_LATER
ACTIONS  : TODO_LATER
ASSETS   : TODO_LATER

CUSTOMER-UI-SNAPSHOT:
  ref: TODO_snapshot